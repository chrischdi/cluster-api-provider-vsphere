---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: fakeapiserverendpoints.vcsim.infrastructure.cluster.x-k8s.io
spec:
  group: vcsim.infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: FakeAPIServerEndpoint
    listKind: FakeAPIServerEndpointList
    plural: fakeapiserverendpoints
    singular: fakeapiserverendpoint
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FakeAPIServerEndpoint is the schema for a VCenter simulator server.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: FakeAPIServerEndpointSpec defines the desired state of the
              FakeAPIServerEndpoint.
            type: object
          status:
            description: FakeAPIServerEndpointStatus defines the observed state of
              the FakeAPIServerEndpoint.
            properties:
              host:
                description: The control plane host.
                type: string
              port:
                description: The control plane port.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
