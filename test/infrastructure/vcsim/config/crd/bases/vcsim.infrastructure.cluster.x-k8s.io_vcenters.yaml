---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: vcenters.vcsim.infrastructure.cluster.x-k8s.io
spec:
  group: vcsim.infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: VCenter
    listKind: VCenterList
    plural: vcenters
    singular: vcenter
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VCenter is the schema for a VCenter simulator server.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VCCenterSpec defines the desired state of the VCenter.
            properties:
              generators:
                description: GeneratorsSpec defines the spec for generators.
                properties:
                  envsubst:
                    description: EnvSubst defines the spec for the envsubst generators.
                    properties:
                      clusters:
                        description: Clusters defines the spec for the envsubst generator
                          targeting specific Cluster API clusters.
                        items:
                          description: ClusterEnvSubstGeneratorSpec defines the spec
                            for the envsubst generator targeting a specific Cluster
                            API cluster.
                          properties:
                            cluster:
                              description: 'Cluster specifies the Cluster for the
                                Cluster API cluster. Default: 0 (C0)'
                              type: integer
                            controlPlaneMachines:
                              description: 'The number of the control plane machines
                                in the Cluster API cluster. NOTE: This variable isn''t
                                related to the vcsim server, but we are handling it
                                here in order to have a single point of control for
                                all the variables related to a Cluster API template.
                                Default: 1'
                              type: integer
                            datacenter:
                              description: 'Datacenter specifies the Datacenter for
                                the Cluster API cluster. Default: 0 (DC0)'
                              type: integer
                            datastore:
                              description: 'Datastore specifies the Datastore for
                                the Cluster API cluster. Default: 0 (LocalDS_0)'
                              type: integer
                            kubernetesVersion:
                              description: 'The Kubernetes version of the Cluster
                                API cluster. NOTE: This variable isn''t related to
                                the vcsim server, but we are handling it here in order
                                to have a single point of control for all the variables
                                related to a Cluster API template. Default: v1.28.0'
                              type: string
                            name:
                              description: The name of the Cluster API cluster.
                              type: string
                            workerMachines:
                              description: 'The number of the worker machines in the
                                Cluster API cluster. NOTE: This variable isn''t related
                                to the vcsim server, but we are handling it here in
                                order to have a single point of control for all the
                                variables related to a Cluster API template. Default:
                                1'
                              type: integer
                          required:
                          - name
                          type: object
                        type: array
                    type: object
                type: object
              model:
                description: VCSimModelSpec defines the model to be used by the VCenter.
                properties:
                  cluster:
                    description: 'Cluster specifies the number of ClusterComputeResource
                      entities to create per Datacenter Name prefix: C, vcsim flag:
                      -cluster Default: 1'
                    type: integer
                  clusterHost:
                    description: 'ClusterHost specifies the number of HostSystems
                      entities to create within a Cluster Name prefix: H, vcsim flag:
                      -host Default: 3'
                    type: integer
                  datacenter:
                    description: 'Datacenter specifies the number of Datacenter entities
                      to create Name prefix: DC, vcsim flag: -dc Default: 1'
                    type: integer
                  datastore:
                    description: 'Datastore specifies the number of Datastore entities
                      to create Each Datastore will have temporary local file storage
                      and will be mounted on every HostSystem created by the ModelConfig
                      Name prefix: LocalDS, vcsim flag: -ds Default: 1'
                    type: integer
                  pool:
                    description: 'Pool specifies the number of ResourcePool entities
                      to create per Cluster Note that every cluster has a root ResourcePool
                      named "Resources", as real vCenter does. For example: /DC0/host/DC0_C0/Resources
                      The root ResourcePool is named "RP0" within other object names.
                      When Model.Pool is set to 1 or higher, this creates child ResourcePools
                      under the root pool. Note that this flag is not effective on
                      standalone hosts. For example: /DC0/host/DC0_C0/Resources/DC0_C0_RP1
                      Name prefix: RP, vcsim flag: -pool Default: 0'
                    type: integer
                  vsphereVersion:
                    description: 'VSphereVersion specifies the VSphere version to
                      use Default: 7.0.0'
                    type: string
                type: object
            type: object
          status:
            description: VCenterStatus defines the observed state of the VCenter.
            properties:
              envsubst:
                description: The output of a envsubst generator.
                properties:
                  clusters:
                    description: the output of a envsubst generator for each Cluster
                      API cluster.
                    items:
                      description: ClusterEnvVars defines the output of a envsubst
                        generator for a specific Cluster API cluster.
                      properties:
                        name:
                          description: name of the Cluster API cluster.
                          type: string
                        variables:
                          additionalProperties:
                            type: string
                          description: variables to use with envsubst when creating
                            the Cluster API cluster.
                          type: object
                      type: object
                    type: array
                type: object
              host:
                description: The vcsim server  url's host.
                type: string
              password:
                description: The vcsim server password.
                type: string
              thumbprint:
                description: The vcsim server thumbprint.
                type: string
              username:
                description: The vcsim server username.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
